package org.example.builderPattern.old;
// Class instances are immutable
// Constructor is too much , unreadable for example
// Objects that need other objects or "parts" to construct them
// In Builder pattern we remove logic related to object construction from "client" code and abstract it in separate classes
public class BuilderPattern {
}
